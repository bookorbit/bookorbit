local _ = require("gettext")
return {
    fullname = _("BookOrbit Sync"),
    description = _([[Synchronizes reading progress, book status, ratings, reading statistics and highlights to a BookOrbit server. The open book syncs automatically on close and suspend; the whole library syncs on demand.]]),
}
